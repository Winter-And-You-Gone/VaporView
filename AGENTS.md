# VaporView Agent Rules

## Default Delivery Rule

在这个仓库中，只要完成了以下任一类型的工作，就把“提交并推送到 GitHub”视为默认收尾步骤，而不是可选步骤:

- 修复 bug
- 新增功能
- 调整构建配置
- 更新文档以匹配代码事实

除非用户在当前对话里明确说“不要提交”或“不要推送”，否则按下面流程执行。

用户已经为本项目追加了一条长期规则:

- 只要修改了代码，就默认在收尾时自动执行“重新构建 -> 提交 -> 推送”，不把这三步拆成可选项单独确认。

## Required End-of-Task Flow

1. 只要改动了代码、构建脚本或会影响可执行结果的资源文件，就必须重新构建，而不是跳过构建。
2. 对于“代码修改”类任务，默认收尾顺序固定为: 重新构建、确认成功、提交、推送。
3. 仅限这个项目，不要在本地挂载目录上执行构建；默认通过 SSH 在远程主机 `Administrator@100.102.21.86` 的 `C:\WorkSpace\NAV\VaporView\build` 目录进行配置和构建。
4. 如果远程 `build` 已存在，就在该目录上增量构建；如果需要重新配置，也在远程仓库根目录 `C:\WorkSpace\NAV\VaporView` 下完成。
5. 只有在远程构建成功后，才允许继续提交并推送；如果构建失败，先修复问题，不要 push 失败状态的改动。
6. 确认需要提交的文件，只包含本次任务相关改动，不回滚用户自己的其它改动。
7. 使用清晰的非交互式 git 命令创建提交。
8. 默认将本次提交推送到当前工作分支；只有用户明确指定目标分支时，才推送到其它分支。
9. 在最终回复里说明:
   - 做了什么
   - 如何验证
   - 使用的构建命令与是否构建成功
   - 提交哈希或提交标题
   - push 是否成功

## Git Safety

- 不要改写历史，除非用户明确要求。
- 不要默认 `--amend`。
- 不要 push 与当前任务无关的改动。
- 如果工作区里混有无关改动，只提交本次任务相关文件。
- 不要把 `Z:\NAV\VaporView` 当作本地构建目录使用；它是远程目录的挂载视图，构建应在远程主机上执行。
- 如果 push 失败，保留本地提交，并在回复里明确说明失败原因和下一步建议。
