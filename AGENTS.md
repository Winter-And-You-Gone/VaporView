# VaporView Agent Rules

## Default Delivery Rule

在这个仓库中，只要完成了以下任一类型的工作，就把“提交并推送到 GitHub”视为默认收尾步骤，而不是可选步骤:

- 修复 bug
- 新增功能
- 调整构建配置
- 更新文档以匹配代码事实

除非用户在当前对话里明确说“不要提交”或“不要推送”，否则按下面流程执行。

## Required End-of-Task Flow

1. 只要改动了代码、构建脚本或会影响可执行结果的资源文件，就必须重新构建，而不是跳过构建。
2. 仅限这个项目，不要在本地挂载目录上执行构建；默认通过 SSH 在远程主机 `Administrator@100.102.21.86` 的 `C:\WorkSpace\NAV\VaporView\build` 目录进行配置和构建。
3. 如果远程 `build` 已存在，就在该目录上增量构建；如果需要重新配置，也在远程仓库根目录 `C:\WorkSpace\NAV\VaporView` 下完成。
4. 只有在远程构建成功后，才允许继续提交并推送；如果构建失败，先修复问题，不要 push 失败状态的改动。
5. 确认需要提交的文件，只包含本次任务相关改动，不回滚用户自己的其它改动。
6. 使用清晰的非交互式 git 命令创建提交。
7. 默认将本次提交推送到 `origin/main`。
8. 在最终回复里说明:
   - 做了什么
   - 如何验证
   - 使用的构建命令与是否构建成功
   - 提交哈希或提交标题
   - push 是否成功

## Git Safety

- 不要改写历史，除非用户明确要求。
- 不要默认 `--amend`。
- 不要 push 与当前任务无关的改动。
- 如果工作区里混有无关改动，只提交本次任务相关文件。
- 不要把 `Z:\NAV\VaporView` 当作本地构建目录使用；它是远程目录的挂载视图，构建应在远程主机上执行。
- 如果 push 失败，保留本地提交，并在回复里明确说明失败原因和下一步建议。
