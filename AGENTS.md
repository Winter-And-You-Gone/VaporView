# VaporView Agent Rules

## Default Delivery Rule

在这个仓库中，只要完成了以下任一类型的工作，就把“提交并推送到 GitHub”视为默认收尾步骤，而不是可选步骤:

- 修复 bug
- 新增功能
- 调整构建配置
- 更新文档以匹配代码事实

除非用户在当前对话里明确说“不要提交”或“不要推送”，否则按下面流程执行。

## Required End-of-Task Flow

1. 先做与本次改动直接相关的验证，例如构建、测试、脚本校验或最小运行检查。
2. 确认需要提交的文件，只包含本次任务相关改动，不回滚用户自己的其它改动。
3. 使用清晰的非交互式 git 命令创建提交。
4. 将当前分支推送到 `origin`。
5. 在最终回复里说明:
   - 做了什么
   - 如何验证
   - 提交哈希或提交标题
   - push 是否成功

## Git Safety

- 不要改写历史，除非用户明确要求。
- 不要默认 `--amend`。
- 不要 push 与当前任务无关的改动。
- 如果工作区里混有无关改动，只提交本次任务相关文件。
- 如果 push 失败，保留本地提交，并在回复里明确说明失败原因和下一步建议。
