# AI-8288 实机交接文档

## 1. 文档目的

本文档记录 AI-8288 实机与 VaporView 当前代码的对比结果，供后续会话、现场联调和代码维护直接使用。

本文档对应的实机型号为：

`AI-8288D92J0G71G71S2-24VDC`

本轮操作全部为 Modbus-RTU 功能码 `03H` 只读请求。没有执行 `06H` 或 `10H` 写入，没有修改 Addr、bAud、Srun、Loc 或任何控制参数。

## 2. 来源与证据

### 2.1 主要说明书

本地说明书：

`D:\Project\GPS\说明书_文档\AI-8+系列多回路智能温控器+使用说明（8xx8g+8xx8精度5以下）.pdf`

说明书版本信息：文档日期为 `2025-12-05`，共 47 页。涉及本交接的内容主要在型号规则、输入/输出参数组、全局参数和 Modbus 协议章节。

### 2.2 代码位置

- 协议地址、编解码和数据结构：[`include/Ai8TemperatureControllerProtocol.h`](../include/Ai8TemperatureControllerProtocol.h)
- 实机读取、写入和实时轮询：[`src/sky/devices/Ai8TemperatureControllerCollector.cpp`](../src/sky/devices/Ai8TemperatureControllerCollector.cpp)
- 温控页面可编辑字段：[`src/ground/widgets/Ai8TemperatureControllerPanel.cpp`](../src/ground/widgets/Ai8TemperatureControllerPanel.cpp)
- AI-8288 串口连接默认配置：[`src/ground/devices/LocalDeviceConnectionController.cpp`](../src/ground/devices/LocalDeviceConnectionController.cpp)
- 既有实机验证清单：[`docs/ai8288_hardware_validation.md`](ai8288_hardware_validation.md)

## 3. 型号解析

根据说明书的型号规则，用户给出的型号可以解析为：

| 型号段 | 含义 | 当前结论 |
| --- | --- | --- |
| `8288` | 8 通道，0.3 级，非隔离输入 | 与代码的 8 通道模型一致 |
| `D92` | 导轨安装，无显示和按键，通过通信或 E85 配置 | 与用户型号一致，软件不能仅凭寄存器验证机械外形 |
| `J0` | 三线制热电阻输入 | 与当前通道 1 的 Pt100 读数相符 |
| 第一个 `G71` | 4 路隔离 NPN 主输出 | 说明书定义存在，未做物理输出带载测试 |
| 第二个 `G71` | 4 路隔离 NPN 辅助输出 | 说明书定义存在，未做物理输出带载测试 |
| `S2` | 固定 RS485 Modbus-RTU | COM1 实测可正常通信 |
| `24VDC` | 24 V 直流供电 | 由用户型号提供，未通过软件验证 |

设备返回的型号特征寄存器 `0x0853` 为 `0x218A`。说明书没有提供该数值与完整型号字符串之间的映射表，所以不能仅凭 `0x218A` 验证 `D92/J0/G71/G71/S2` 每一个后缀。

## 4. COM1 通信实测

### 4.1 连接环境

- Windows 串口：`COM1`
- USB 转串口芯片：Silicon Labs CP210x USB to UART Bridge
- Windows 端口状态：正常
- 实测打开参数：19200 baud、8 数据位、无校验、1 停止位
- Modbus 站号：1

曾对说明书列出的波特率 `4800/9600/19200/38400/57600/115200`，以及无校验/偶校验、1/2 停止位组合进行只读探测。只有 19200 得到有效的型号特征响应。需要注意：设备寄存器不保存停止位，串口容错也可能使不同停止位组合得到响应，因此“1 个停止位”主要由当前 Windows 端口设置确认，而不是由设备寄存器确认。

### 4.2 当前全局寄存器快照

读取范围：`0x0840-0x0855`，功能码 `03H`。

| 地址 | 字段 | 原始值 | 解码值 | 备注 |
| --- | --- | ---: | ---: | --- |
| `0840` | `Addr` | `0x0001` | `1` | 当前站号 |
| `0841` | `bAud` | `0x00C0` | `19200` | 原始值按 0.1K 解码 |
| `0842` | `Adn` | `0x0008` | `8` | 说明书字段，代码未映射 |
| `0843` | `ACH` | `0x0008` | `8` | 说明书字段，代码未映射 |
| `0844` | `Ctn` | `0x0008` | `8` | 控制回路数 |
| `0845` | `Srun` | `0x0001` | `1` | 说明书未定义该值，见第 7 节 |
| `0846` | `CtI` | `0x0004` | `0.4 s` | 原始值按 0.1 s 解码 |
| `0847` | `ALAL` | `0x0018` | `24` | 说明书字段，代码未映射 |
| `0848` | `ALCH` | `0x0004` | `4` | 说明书字段，代码未映射 |
| `0849` | `ALbt` | `0x000F` | `15` | 说明书字段，代码未映射 |
| `084A` | `AFA` | `0x0000` | `0` | 说明书字段，代码未映射 |
| `084B` | `AFB` | `0x0000` | `0` | 说明书字段，代码未映射 |
| `084C` | `AFC` | `0x0000` | `0` | bit 0 表示无校验，其余选项均为 0 |
| `084D` | `Nonc` | `0x0000` | `0` | 说明书字段，代码未映射 |
| `084E` | `EAF` | `0x0000` | `0` | 当前代码字段名为 SampleMode |
| `084F` | `EHYS` | `0x0014` | `2.0` | 原始值按 0.1 解码 |
| `0850` | `dPt` | `0x0001` | `1` | 小数点显示位数 |
| `0851` | 主状态 | `0x0200` | 原始值保留 | 按说明书状态位表示有全局告警 |
| `0852` | `Loc` | `0x0000` | `0` | 当前未锁定 |
| `0853` | 型号特征 | `0x218A` | 原始值保留 | 无官方型号映射表 |
| `0854` | 机号高字 | `0x2000` | - | 说明书字段 |
| `0855` | 机号低字 | `0x1919` | - | 说明书字段 |
| - | 当前代码原始拼接值 | - | `0x20001919` | 高字左移 16 位后与低字拼接，最终显示格式待确认 |

### 4.3 `0x0856-0x085F` 扩展寄存器

这些字段在说明书中属于扩展、工厂或调试相关区域。已读取但没有写入，也没有将其解释为可配置功能。

| 地址 | 说明书字段 | 原始值 |
| --- | --- | ---: |
| `0856` | `OPCH` | `1` |
| `0857` | `FL32` | `20` |
| `0858` | `AIF1` | `150` |
| `0859` | `AIF2` | `15` |
| `085A` | `P1FA/AIF3` | `9999` |
| `085B` | `dIFA` | `2` |
| `085C` | `SPSr` | `100` |
| `085D` | `AtFn` | `55` |
| `085E` | `AIFL/P1Pr` | `0` |
| `085F` | `P1TI/OPSn` | `0` |

## 5. 通道参数实测

### 5.1 通道公共参数

| 寄存器组 | 通道 1 | 通道 2-8 | 解码说明 |
| --- | --- | --- | --- |
| `SP 0000-005F` | `30.0` | `100.0` | 0.1 单位 |
| `PV 0600-065F` | `30.0` | `911.0` | 0.1 单位；本次仅通道 1 连接测温探头，通道 2-8 未连接且输入错误报警位有效 |
| `P 0060-00BF` | `30.0` | `30.0` | 0.1 单位 |
| `I 00C0-011F` | `100.0 s` | `100.0 s` | 0.1 s |
| `d 0120-017F` | `50.00 s` | `50.00 s` | 0.01 s，有符号 |
| `In 0180-01DF` | `1` | `1` | 使用输入参数组 1 |
| `Sc 01E0-023F` | `0` | `0` | 测量平移 |
| `On 0240-029F` | `0` | `0` | 使用输出参数组 1 |
| `Pn 02A0-02FF` | `0` | `0` | 说明书字段，代码未映射 |
| `At 0300-035F` | `0` | `0` | 当前为 APID/自动控制模式 |
| `OP 0360-03BF` | 原始 `1140` | `0` | `At=0` 时为自动输出，不应当按手动设定值写入 |
| `HA 03C0-041F` | `3200.0` | `3200.0` | 0.1 单位 |
| `LA 0420-047F` | `-999.0` | `-999.0` | 0.1 单位，有符号 |
| `SV 0480-04DF` | 与 `SP` 相同 | 与 `SP` 相同 | 0.1 单位 |

`OP` 原始值 `1140 / 256` 约为 `4.45%`，但由于当前 `At=0`，这是自动控制输出快照，不是可编辑的手动输出设定。代码已经只在 `At=3` 时写入 `OP`。

### 5.2 输入参数组

| 寄存器组 | 组 1 | 组 2 | 组 3 | 组 4 |
| --- | ---: | ---: | ---: | ---: |
| `InP 0800-0803` | `21`，Pt100 | `0`，K | `0`，K | `0`，K |
| `ScL 0804-0807` | `0` | `0` | `0` | `0` |
| `ScH 0808-080B` | `1000.0` | `1000.0` | `1000.0` | `1000.0` |
| `FIL 080C-080F` | `0` | `0` | `0` | `0` |

当前 8 个通道的 `In=1`，因此实际控制都引用输入组 1。输入组 2-4 的默认值不代表对应物理端已经接入传感器。

### 5.3 输出参数组

4 个输出组读取结果相同：

| 字段 | 原始/解码值 |
| --- | --- |
| `dHA` | `3200.0` |
| `dLA` | `-999.0` |
| `AAF` | `0` |
| `HYS` | `2.0` |
| `OPL` | `0%` |
| `OPH` | `100%` |
| `OHE` | `3200.0` |
| `Act` | `0`，加热/反作用 |
| `Srh` / `SrL` | `0 / 0` |
| `SPL` / `SPH` | `-999.0 / 3200.0` |

## 6. 状态与报警

### 6.1 报警寄存器 `0x0680-0x0683`

原始值为：

`0x0001, 0x0101, 0x0101, 0x0101`

说明书定义每个字节对应一个通道，bit 0 为输入错误。因此：

- 通道 1：无输入错误
- 通道 2-8：均置输入错误位

本次仅通道 1 连接了测温探头，因此通道 2-8 读到 `911.0`，同时对应输入错误报警位有效。`911.0` 在本次设备和接线条件下表示未连接测温探头或温度无效，但说明书没有规定它是通用的固定错误代码。软件必须以报警寄存器判断输入是否有效，不能仅凭 PV 数值判断。

### 6.2 控制状态寄存器 `0x06C0-0x06C3`

原始值为：

`0x0101, 0x0101, 0x0101, 0x0101`

按说明书的高低字节规则解码，8 个通道均为：

- bit 0 = 1：非自整定/APID
- bit 1 = 0：未停止

因此设备当前通道控制状态正常运行，但全局 `Srun=1` 的语义仍未确定。

### 6.3 高分辨率 PV `0x0660-0x066F`

已读取原始 16 位字，但说明书没有清楚说明 32 位组合顺序、符号处理和缩放方式。当前项目没有使用该区域，后续不要将它直接换算成温度，也不要据此修改普通 PV 处理。

## 7. 说明书、代码和实机之间的未决项

### 7.1 `Srun=1` 未定义，软件层误写风险已修复

说明书列出的运行状态主要是 `0`、`15` 和 `9655`，实机返回 `1`。`1` 的设备语义仍未确认，不能将其推断为自动运行。

软件层的误写风险已经修复：

- 页面保留设备读取到的 Srun 原始 16 位值，并在未识别时显示十进制和十六进制原值。
- 保存 CtI、Ctn、EAF、dPt、Loc 等其它全局参数时，不会把未知原值转换为 `0`，也不会写入 `0x0845`。
- 只有用户明确选择说明书支持的 `0`、`15` 或 `9655`，且目标值与设备当前值不同，才会考虑写入 `0x0845`。
- Collector 后端独立拒绝未被说明书支持的 Srun 目标值，并在任何其它寄存器写入前返回失败。

相关代码：

- UI 运行状态选项：[`src/ground/widgets/Ai8TemperatureControllerPanel.cpp`](../src/ground/widgets/Ai8TemperatureControllerPanel.cpp)
- 全局页读取和写入：[`src/sky/devices/Ai8TemperatureControllerCollector.cpp`](../src/sky/devices/Ai8TemperatureControllerCollector.cpp)

在确认 `1` 的设备语义前，软件会显示并保留该原始值；后续实机仍需确认值 `1` 的真实含义。

### 7.2 J0 输入类型与 21/22 的应用选择待确认

当前实机 `InP1=21`，说明书明确将其定义为 Pt100，范围 `-200～+800 °C`。因此，当前代码使用 21 表示 Pt100 并非自行猜测。说明书同时规定：

- `22`：Pt100，范围 `-200.00～+300.00 °C`，分辨率更高
- `23`：Pt1000
- `24`：`0～2000 Ω` 电阻输入

尚未确认现场应用是否更适合使用 21 还是 22，当前 UI 已提供 21 和 23，但尚未覆盖 22 和 24。需要通过已知温度点、实际量程和现场传感器规格继续验证；在确认前禁止自动替换输入规格，不能把 23 或 24 视为 Pt100 的等价编码。

### 7.3 停止位未存储在设备寄存器

当前设备按 COM1 的 Windows 配置使用 8N1。代码也固定使用 `SerialConfig::N81`。说明书允许 1 或 2 个停止位，但只读寄存器无法区分两者；后续若开放串口参数编辑，需要把设备切换时序和主机重连流程作为独立实机测试。

### 7.4 型号特征字没有官方映射

`0x0853=0x218A` 只能证明设备返回了一个型号特征字。当前代码只记录数值，不会将它与用户输入的完整型号比较，也无法验证两个 `G71` 输出模块的物理形态。

### 7.5 说明书地址范围自相矛盾

协议概览处写站号范围 `0..80`，全局参数表又出现 `0..88`。当前 UI 使用 `1..88`。这台设备地址为 1，不受影响，但新增地址校验时不能只引用其中一处文字。

### 7.6 当前代码没有覆盖全部参数

协议头已定义主要地址，但采集器和 UI 只覆盖其中一部分：

- 通道页未显示 `Pn`、`HA`、`LA`、`SV` 等完整通道参数
- 输出页未显示 `dHA`、`dLA`、`OHE`、`SPL`、`SPH`
- 全局页未显示 `Adn`、`ACH`、各报警公共参数、`EHYS`、主状态、型号特征和序列号
- 实时轮询只读取普通 PV 和控制状态，没有读取报警寄存器、主状态和高分辨率 PV

代码与说明书的基础地址、符号缩放、报警字节映射和控制状态位映射基本一致，但“协议已定义”不等于“页面已经覆盖”。

## 8. 后续会话的安全操作规则

1. 连接该设备优先使用 `COM1 @ 19200, 8N1, Modbus 地址 1`。
2. 初次联调只使用功能码 `03H` 读取 `0x0853`、`0x0600-0x0607`、`0x0680-0x0683` 和 `0x06C0-0x06C3`。
3. 通道 2-8 本次的 `PV=911.0` 与未连接测温探头和输入错误状态同时出现；不要把 911.0 当作通用错误码或有效温度，输入有效性必须以报警寄存器为准。
4. 未确认 `Srun=1` 语义前，不要通过当前全局页面保存参数。
5. 不要写入 `0x0840/0x0841`，除非已经准备好设备切换通信参数后的主机重连流程。
6. 不要写入 `0x0660` 高分辨率 PV 区，也不要修改 `0x0856-0x085F` 扩展寄存器。
7. 修改任何参数时只改一个字段，写入后立即用 `03H` 回读，并记录原始值、解码值和设备状态。
8. 任何现场日志、截图、导出包和测试样例不得提交到 Git；本文件只保留必要的寄存器快照和结论。

## 9. 建议的后续修复与验收标准

### 优先级 P0：避免误写 `Srun`（软件层已修复）

- 软件层误写风险已修复；读取到未识别的运行状态时，UI 显示并保留原始值。
- 只有用户明确选择 `0`、`15` 或 `9655` 时才允许写入；后端会再次校验目标值。
- 自动化回归测试覆盖实机返回 `Srun=1` 后保存其它全局字段不得写入 `0x0845`。
- `Srun=1` 的设备语义仍未知，后续实机仍需单独确认。

### 优先级 P1：补齐实机诊断信息

- 在只读诊断页显示 `0x0853`、序列号、主状态、报警寄存器和控制状态。
- 显示通道 `In/Sc/On/Pn/HA/LA/SV` 以及输出 `dHA/dLA/OHE/SPL/SPH`。
- 保留未知寄存器的原始十六进制值，不要在没有说明书依据时强行解码。

### 优先级 P1：确认 J0 输入码

- 使用已知 Pt100 温度点分别确认 21 和 22 的量程、负温度和三线制补偿行为。
- 在确认前不要因为用户型号包含 `J0` 就自动把 21 替换为 22；也不要把 23 或 24 视为 Pt100 等价编码。

### 优先级 P2：完善型号和协议校验

- 收集原厂对 `0x218A` 的型号特征字映射。
- 将站号范围冲突记录为兼容性规则，明确 UI、协议和说明书的最终采用范围。
- 若未来开放偶校验或停止位切换，必须加入串口重连和通信恢复的实机测试。

## 10. 一句话结论

这台设备已经确认可以在 `COM1 / 19200 / 8N1 / 地址 1` 下稳定读取，代码的基础 Modbus 地址和常用参数缩放基本正确；软件层的 `Srun=1` 误写风险已修复，但该值的设备语义仍需实机确认，另外报警、完整通道参数、完整输出参数和全局扩展字段尚未进入当前页面。
