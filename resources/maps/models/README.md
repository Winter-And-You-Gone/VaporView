# Local 3D Models

This directory is reserved for optional local 3D models used by the native 3D Map.
Model payloads are intentionally ignored by Git so large assets stay local.

Current supported slot:

- `aircraft/`: optional aircraft marker override.

