# Local Imagery

This directory is reserved for future local raster imagery layers.

Current built-in templates use Natural Earth as the offline visual background and optional local DEM/OSM layers. Put large GeoTIFF/VRT imagery here only for local testing; these files are ignored by git.
